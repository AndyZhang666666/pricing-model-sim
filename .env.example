# 本项目不需要任何 API key。
#
# 所有计算（留存拟合、三种付费机制推算、敏感性扫描）都是纯数学，在浏览器端
# 或本地 Node 里跑，不调用任何外部服务。这个文件的存在只是为了明确这一点，
# 免得有人以为要先配好 key 才能跑起来。
#
# 如果你 fork 之后想加 AI 相关的功能（比如让模型帮忙估一条留存曲线），
# 走 BYO-key：访客在页面设置里填自己的 key，存 localStorage，直连 OpenAI 兼容
# 接口。不要把你的 key 打进代码或构建产物。
#
# 参考格式（当前项目用不到，留作模板）：
# LLM_BASE_URL=https://your-endpoint.example.com/v1
# LLM_API_KEY=sk-replace-me
